@model MyDMVpro.Models.AppFormModels.AppFormViewModel

@{
    await Html.RenderPartialAsync("_vinChange");
    await Html.RenderPartialAsync("_FormFill", Model);
}

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}

